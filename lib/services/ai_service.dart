class AIService {
  static Future<String?> generateAIQR({
    required String qrData,
    required String prompt,
  }) async {
    await Future.delayed(const Duration(seconds: 2));
    return null;
  }
}